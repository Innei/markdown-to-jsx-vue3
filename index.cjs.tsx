import Markdown, {
  compiler,
  Priority,
  anyScopeRegex,
  blockRegex,
  inlineRegex,
  parseBlock,
  parseInline,
  parseSimpleInline,
  parseCaptureInline,
  sanitizeUrl,
  simpleInlineRegex,
} from './'
Object.assign(Markdown, {
  compiler,
  Priority,
  anyScopeRegex,
  blockRegex,
  inlineRegex,
  parseBlock,
  parseInline,
  parseSimpleInline,
  parseCaptureInline,
  sanitizeUrl,
  simpleInlineRegex,
})
export default Markdown
